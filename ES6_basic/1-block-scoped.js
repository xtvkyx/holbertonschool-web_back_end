export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
    const taskBlock = true;
    const task2Block = false;
  }

  return [task, task2];
}
